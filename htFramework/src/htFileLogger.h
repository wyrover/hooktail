#pragma once 

#include <iostream>
#include <fstream>

class htFileLogger
{
public:
                            htFileLogger() {}
                            ~htFileLogger() {}
/*
    VOID                    Log(const CHAR* str);

private:
    std::fstream            m_file;
*/
};